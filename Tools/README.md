# Tools

This directory contains local utility tooling for FTS development.

## Current layout

- `fts_toolkit_map_wizards/` (in development): Python package and CLI for creating and validating FTS map entities (`mapRegion`, `mapLocale`, `mapPoint`, `mapRoute`, `mapAgent`).

## Usage

See `Tools/fts_toolkit_map_wizards/README.md` for setup and command usage.
